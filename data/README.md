This directory stores the dataset paths used by Point-UQ.

Expected entries:

- `data/shapenet`
- `data/co3d`
- `data/modelnet`
- `data/scanobjnn`

Each entry may be a real directory or a symbolic link created with `prepare_assets.sh`.
